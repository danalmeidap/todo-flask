{% extends "base.html.j2" %}
{% block content %}
<div class="container">
<h1>{{ task.title }}</h1>

<div class="jumbotron">
<small>{{ task.date | format_date }}</small>  
<p>
    {{ markdown(task.content) }}   
</div>
</div>
{% endblock %}